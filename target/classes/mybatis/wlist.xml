<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="wlist">
	<insert id="winsert" parameterType="vo.Wlist">
	INSERT INTO WATCH_LIST (MEMBER_ID. MOVIE_ID) VALUES(#{MEMBER_ID},#{MOVIE_ID})
	</insert>  
	
	<select id="selectWlist" resultType="vo.Wilist">
	SELECT MI2.POSTER_URL WL.MOVIE_ID, MI2.MOVIE_TITLE  FROM WATCH_LIST WL , MEMBER_INFO MI , MOVIE_INFO MI2 
	WHERE WL.MEMBER_ID = MI.MEMBER_ID AND WL.MOVIE_ID = MI2.MOVIE_ID;
	</select>
  </mapper>